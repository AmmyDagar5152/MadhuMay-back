BACKEND_PORT=8001
MONGO_URL="mongodb+srv://dastrowork_db_user:dxtASuHoejwCEUrd@madhumay.1dxsddv.mongodb.net"
DB_NAME=vrndavan
CORS_ORIGINS=*

# Auth
JWT_SECRET=vrndavan-please-set-a-real-secret
# Emails on this list get isAdmin=true automatically on sign in
ADMIN_EMAILS=admin@vrndavan.example

# Google OAuth (leave empty to disable the button). Populate with credentials from Google Cloud Console.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Public URL that Google will redirect to. Must match a redirect URI in your OAuth client.
GOOGLE_REDIRECT_URI=
# Where the frontend lives — backend redirects here after Google flow completes.
# Leave empty to fall back to the incoming request's origin (works for any deployment target).
FRONTEND_URL=
